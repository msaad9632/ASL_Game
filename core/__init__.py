"""Shared ASL recognition engine — imported by every scenario, never duplicated.

This package is theme-agnostic: it does capture, normalization, the sign schema, and the
generic temporal verifier. Scenario folders own presentation only. See CLAUDE.md.
"""
