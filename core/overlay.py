"""Dev-only debug overlay — live per-parameter scores.

Implemented in Phase 5. Draws the per-parameter confidences (handshape/location/movement/
orientation) onto the frame behind a --debug flag, so failures are explainable, not guessed.
"""
