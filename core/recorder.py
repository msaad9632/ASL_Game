"""Record live normalized frames to a JSON fixture.

Implemented in Phase 4. Captures N seconds of normalized Frames and writes them to a JSON
fixture for replay in the confusor test suite (and, later, as our own training data).
"""
