"""CLI: record a few seconds of live landmarks to a JSON fixture.

Implemented in Phase 4. Wraps core.recorder so we can capture correct-sign and confusor
fixtures for the test suite. Usage (Phase 4):

    python -m tools.record_fixture --name coffee_correct --seconds 3
"""
