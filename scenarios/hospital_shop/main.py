"""Hospital scenario entry point (teammate's workspace).

Scaffold only in v1. Mirror the coffee-shop structure: read webcam -> core.capture -> core
RollingBuffer -> core.verifier.verify(active_sign) -> drive this scenario's scene.py. Reuse
core/ and signs/ unchanged; only the theme/assets differ.
"""
